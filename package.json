{
  "name": "lob-sdk",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "npm run generate:scenario-index && tsc -p tsconfig.build.json",
    "generate:scenario-index": "node scripts/generate-scenario-index.cjs",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "cli-table3": "^0.6.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "simplex-noise": "^4.0.3"
  }
}
