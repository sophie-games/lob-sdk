{
  "name": "lob-sdk",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "prepare": "npm run generate:scenario-index",
    "build": "npm run generate:scenario-index && tsc -p tsconfig.build.json",
    "generate:scenario-index": "node scripts/generate-scenario-index.cjs",
    "pretest": "npm run generate:scenario-index",
    "test": "jest",
    "pretest:watch": "npm run generate:scenario-index",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "cli-table3": "^0.6.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "simplex-noise": "^4.0.3"
  }
}
