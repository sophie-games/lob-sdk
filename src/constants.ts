/**
 * 2 * Math.PI
 */
export const TWO_PI = 6.283185307179586;

/**
 * Math.PI / 180
 */
export const DEG_TO_RAD = 0.017453292519943295;

export const MIN_COLLISION_LEVEL = 0;
export const NO_COLLISION_LEVEL = -1;

/** Unit type IDs >= this value are custom (scenario-defined) rather than built-in. */
export const CUSTOM_UNIT_TYPE_OFFSET = 10000;
